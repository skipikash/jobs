# comment a

triggers:
  pushes:
    branches:
      - "*"
    tags:
      - "*"
    paths:
      exclude:
        - "docs/**"
      # include:
      #   - "**/*.java"

  prs:
    branches:
      - "*"

jobs:
  build:
    when: ${{git.ref == "develop"}}
    call: ./my-build-job.yml
    inputs:
      parameter1: ${{env.ABC}}
    stages:
      - run: echo "hello"

  deploy:
    depends-on: [build]
    stages:
      - run: echo "hello"

      - run: echo "goodbye"
        when: ${{ env.SAY_GOODBYE }}
